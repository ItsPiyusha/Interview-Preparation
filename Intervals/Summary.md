💡 Patterns to remember for interview speed:

1. Always sort — by start time for merging, by end time for scheduling.

2. Two-pointer or heap — when you need to track overlaps.

3. Sweep line — convert to “events” (+1 for start, -1 for end).

4. Interval DP — break into subproblems like dp[l][r].